suppressMessages(source("./scripts/mutationsFunctions.R"))
suppressMessages(source("./scripts/argparseScript.R"))
suppressMessages(library(rmarkdown))

# argument parser for script
time <- executionTime()

# Data generated by the parser
parserData <- mainScriptParser()
# Analyse sequences
main(parserData)

print(executionTime(time))

